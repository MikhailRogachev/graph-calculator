<UserControl x:Class="ruby_plotter.app.UserControls.RangeSetting"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ruby_plotter.app.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Width="250">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="75"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Grid.Row="0" 
            Grid.ColumnSpan="4" 
            Text="Period"
            Style="{StaticResource SettingTextBlock}" />

        <TextBlock 
            Grid.Row="1" 
            Grid.Column="0" 
            Text="From:"
            Style="{StaticResource SettingTextBlock}" />

        <TextBox 
            Grid.Row="1" 
            Grid.Column="1" 
            Text="{Binding xMin, Mode=TwoWay}"
            Style="{StaticResource SettingTextBox}"/>

        <TextBlock 
            Grid.Row="1" 
            Grid.Column="2" 
            Text="To:"
            Style="{StaticResource SettingTextBlock}" />

        <TextBox 
            Grid.Row="1" 
            Grid.Column="3" 
            Text="{Binding xMax, Mode=TwoWay}"
            Style="{StaticResource SettingTextBox}"/>
    </Grid>
</UserControl>
